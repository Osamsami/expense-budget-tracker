# Expense Budget Tracker

A full-stack expense and budget tracking application.

## Setup

1. Clone the repository.
2. Provide a `.env` file in the `backend/` directory (see backend setup).
3. Run `npm install` inside the `backend/` directory.
4. Run `npm start` to start the backend.
